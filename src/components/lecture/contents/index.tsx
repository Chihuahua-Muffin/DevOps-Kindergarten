import One, { Checkpoints } from './one';
import type { LectureContent } from './types';

const LectureContents: LectureContent[] = [
  {
    lectureNumber: 1,
    sliderContents: One,
    checkpoints: Checkpoints,
  },
];

export default LectureContents;
